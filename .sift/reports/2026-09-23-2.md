# sift audit — legacy-forever @ 6f60659

Scope: whole repo. Gate before: pass. Gate after: pass (StyLua, Ruff format/lint, luacheck, LuaLS and the
multi-value lint, three specs, actionlint, zizmor, gitleaks, `sift check`, `sift agents check`).
`Data/Legacy.lua` regenerates byte-identical (default and `--offline`); the screenshots are
deterministic across two runs.

## Slices

Two blind reviewers (Claude and Codex) per slice, 11 lenses each. All 10 envelopes validate at `6f60659`.

| Slice | Reported | Changed | Proposed | Dismissed |
|---|---|---|---|---|
| `tools/` | 8 | 7 | 1 | 1 |
| `Core.lua`, `Tracker.lua`, `tests/` | 2 | 1 | 1 | 0 |
| `Model.lua`, `Live.lua` | 2 | 2 | 0 | 0 |
| `Completion.*`, `Map.*` | 3 | 1 | 2 | 0 |
| docs + config | 9 | 7 | 1 | 0 |

## Changes

| Slice | Change | Category |
|---|---|---|
| tools | `download()` validates the CSV once for both the cached and the fetched path | parallel |
| tools | `CURATED_ID`/`EVIDENCE` regexes named once (were 3 and 2 copies); redundant `CRITERIA_KINDS` lookup | parallel |
| tools | `named_refs()` renders raid, dungeon and legacy entries | parallel |
| tools | `screenshots.py` imports `COMPLETION_CATEGORIES`; dead `LABELS`, always-None `hover`, unused return | parallel / dead |
| tools | `lint_multivalue.py` listed its operator set twice | parallel |
| tools | map screenshot drew dungeon entrances as bare shields; now the portal with a shield badge, as `Map.lua` Layout does since #27 (`map.png` redrawn) | stale-docs |
| core-tests | `data_spec` iterates `ns.Model.COMPLETION_CATEGORIES` instead of a literal list | parallel |
| model-live | two stale comments (Criteria type 8; category shape missing `pending`/`complete`) | comment |
| ui | `zone.areas or {}` / `zone.taxis or {}` in Audit: every category is always emitted and `data_spec` asserts it | defensive-noise |
| docs-config | `.pkgmeta` dot entries: the pinned packager prunes every dot path itself (`release.sh` copy step) | dead config |
| docs-config | README: taxis are matched by the zone in their name; Valthalak's quest has a reviewed entrance pin (store copy too) | stale-docs |
| docs-config | tools/README: subset-remap fallback, `latest_build.py` no longer a verbatim copy, `continent.png` | stale-docs |
| docs-config | sift-project profile: sift gate row, uvx forms, ty count, live roots, SavedVariables question, packager rule | profile |

The previous report's note that `.agents` "would have shipped in the zip" was wrong: the packager prunes
dot-prefixed paths before `.pkgmeta` ignores apply.

## Proposed (not changed)

- **Bug:** `Completion.Of` (`Completion.lua:116`) returns nil when `total == 0` but `pending > 0` (only
  Flight paths counted, no flight master opened yet), so the zone shows nothing instead of the documented "?".
  The fix needs a percent display for an all-pending zone.
- Menu tags `MENU_LEGACY_HERE`, `MENU_LEGACY_HERE_TRACKER`, `MENU_LEGACY_HERE_ZONE_TRACKER` keep the old
  name, but they are public `Menu.ModifyMenu` keys since v0.1.0; renaming would break any addon that hooks them.
- `tools/gen_legacy.py` (1039 lines): move the `locations.json` curation checks (about lines 446–612) to
  `tools/curation.py` in a change of its own. Criteria.Type magic numbers go with it.
- README line 21: a 197-word Zone completion bullet; move the detail to Usage / `docs/zone-completion.md`.
- Carried forward: multi-projection entrances dropped silently; `showAreas` bypassing `SavedTable`;
  `IndexOf`/count loops vs FrameXML helpers (only `tContains`, `Mixin` confirmed).
- Deferred for open PR #28 (Map.lua): `map:GetMapID()` at line 337, the tile-size guard at 594 (with
  non-optional `tileWidth`/`tileHeight` in `types/addon.lua`), hard-coded `RAIDS` at line 11.

## Dismissed

vulture `rep` (lambda argument); jscpd `Completion.xml` template blocks; inventory marking `Model.lua` and
`tools/legacy_render.py` as generated; `FLIGHT_MASTER` vs `SNAPSHOT_CHANGES` (distinct roles);
`Completion.lua:112` nil check (keeps LuaLS narrowing).
