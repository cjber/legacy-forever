# sift audit — legacy-forever @ 9a543c4

Scope: whole repo. Gate before: pass. Gate after: pass (StyLua, Ruff format/lint, luacheck, LuaLS and
the multi-value lint, three specs, actionlint, zizmor, gitleaks, `sift check` with the known
over-1000-lines note, `sift agents check` with the standards pack). `Data/Legacy.lua` and the four
screenshots regenerate byte-identical before and after each `tools/` change.

## Carried forward

| Item from 2026-09-23 / 2026-09-23-2 | Disposition |
|---|---|
| `tests/model_spec.lua` OverlayRect asserted by sum | fixed: each field asserted |
| `Completion.lua:116` pending-only zone returns nil | still present (bug, below) |
| `MENU_LEGACY_HERE*` menu tags | dismissed: public menu API |
| `gen_legacy.py` over 1000 lines, curation checks a split candidate | still present |
| `Criteria.Type` magic numbers 8/27/165/0/78 in `gen_legacy.py` | still present |
| README Zone completion bullet (~197 words) | still present; PR #28 edits the adjacent line |
| `entrances()` drops multi-projection entrances silently | still present |
| `Map.lua` `showAreas` bypasses `ns.SavedTable`; `:337` `map` local; `:594` tile guard; `RAIDS` | deferred: open PR #28 edits `Map.lua` |
| `IndexOf`/`CountTable` vs FrameXML helpers | unproven: helpers unconfirmed on this client |
| compact/expand render duplicate | fixed: `render_entry` |
| `latest_build.py` User-Agent | fixed |
| category list in three places | fixed |
| SavedVariables timing | still open |
| `FLIGHT_MASTER` vs `SNAPSHOT_CHANGES` | dismissed: distinct roles |

## Slices

Codex reviewer per slice plus my own read (Claude subagents were at the concurrency limit). All eight
envelopes in `.sift/runs/` validate.

| Slice | Reported | Proven | Changed | Dismissed |
|---|---|---|---|---|
| `tools/gen_legacy.py`, `legacy_render.py` | 1 | 1 | 1 | 0 |
| `tools/` checks + screenshots | 3 | 2 | 2 | 1 (Literal kind: outside permitted edits) |
| `Core.lua`, `Tracker.lua`, `tests/` | 0 | — | — | — |
| `Model.lua`, `Live.lua` | 1 | 1 | 0 | 1 (LuaLS narrowing) |
| `Completion.*`, `Map.*` | 1 | 1 (bug) | 0 | 0 |
| docs + config | 2 | 2 | 0 | 0 (both overlap PR #28) |
| standards (WFA-1..17) | 8 | 7 | 0 | 1 (WFA-12: CHANGELOG keeps old entries verbatim) |

## Changes

| Slice | Change | Category | Lines |
|---|---|---|---|
| tools | `explore()` `if art_zones` guard is always true; `r = assignment` alias; unused `zone=None` default | impossible branch / dead | −2 |
| tools | `screenshots.py`: three `or {}` around a reader that always returns a dict; `unlocated_tree` docstring shape; stale capture-account note | impossible branch / stale-docs / comment | 0 |
| model | `Model.REPUTATION_TARGET` read only in `Model.lua`, now a local | dead export | 0 |
| docs | `tools/README.md` simulated-bump narration stated as behaviour | session-residue | −1 |

Net: −3 lines across 4 files (`gen_legacy.py` 1039 → 1037).

## Bugs found, not fixed

| Location | Evidence | Suggested fix |
|---|---|---|
| `Completion.lua:116` | `result.total > 0 and result or nil`: with only flight paths counted and no flight master opened, every item is pending, so the zone shows nothing instead of its pending line | return the result when `pending > 0` too; make `PercentText` handle a nil percent |
| `README.md:18` (+ `docs/curseforge.md`) | says "No fixed location" challenges list every unfinished step; `Tracker.lua` `MAX_STEPS = 5` shows five then "..." | say "up to five"; after PR #28 lands |
| `README.md:19` | "Map pins on dungeon entrances"; raid entrances (Onyxia) are pinned too | "dungeon and raid entrances"; after PR #28 |
| `tools/screenshots.py:657` (WFA-9) | tracker zone block renders counts only; `Completion.lua:252-264` adds up to five remaining objectives with icons | render `category.left` lines, regenerate `tracker.png` |
| `Core.lua:118` (WFA-3) | bare `/lf` prints help; compartment click only toggles the map | open the map's Legacy menu from both |
| `Core.lua:30` (WFA-4) | `ns.Print` uses custom `|cff33ff99` | a stock font colour |
| `README.md` (WFA-10) | no "Works alongside" section | add one with verified coexistence only |
| `Completion.lua:315` (WFA-14) | `trackerCollapsed`/`mapCollapsed` defaults live in inline comparisons; `showAreas` in `Map.lua` | declare all defaults in one table |
| `media/icon.svg` (WFA-7) | parchment map behind the pin, not the template's single gold emblem | redraw to template |

## Unproven candidates

| Location | Candidate | What would settle it |
|---|---|---|
| `Completion.lua:606` | `Audit` restates `ForFaction` without its `nil` side case | confirm every taxi has `faction`, then share `ForFaction` |
| `tools/lint_multivalue.py:15` | `Token.kind` is a closed set typed as `str` | owner call: a `Literal` is a type change, not a permitted audit edit |
| `tools/README.md:166` | "Changes from the previous slice" table mixes history ("Newly located") with standing facts | owner rewrite |

## Learning captured

- Rules added: none.
- Project skill updates: regenerate-and-diff proof recipe for `tools/` (cache location, `--offline`);
  `SIFT_STANDARDS_PATH` for the standards pack; anti-patterns (`or {}` on non-nullable mock readers,
  session narration in `tools/README.md`); LuaLS narrowing guards and uiOrder `0` are not findings.

## Next

Fix the `Completion.Of` pending-only bug, then fold the README/tracker doc corrections in after PR #28.
