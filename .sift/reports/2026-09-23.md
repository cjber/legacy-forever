# sift audit — legacy-here @ 3406183

Scope: whole repo. Gate before: pass. Gate after: pass (StyLua, Ruff format/lint, luacheck, LuaLS, both specs, actionlint, zizmor, gitleaks).

## Slices

Five blind reviewers, one per slice, 13 lenses each (zone-filtered). All 54 envelopes validated CLEAN/FINDINGS at their review head; they now read INCOMPLETE only because the slice commits moved HEAD.

| Slice | Files read | Reported | Proven | Changed | Unproven | Gate |
|---|---|---|---|---|---|---|
| `tools/` | 5/5 | 13 | 2 | 2 | 11 | pass |
| `Core.lua`, `Tracker.lua`, `tests/` | 4/4 | 4 | 0 | 0 | 4 | pass |
| `Model.lua`, `Live.lua` | 2/2 | 5 | 3 | 3 | 2 | pass |
| `Completion.*`, `Map.*` | 4/4 | 8 | 3 | 3 | 5 | pass |
| docs + config | 13/13 | 11 | 5 | 5 | 6 | pass |

## Changes

| Slice | Change | Category | Lines |
|---|---|---|---|
| model-live | `CompletionCategory` always-true guard; `taxis or {}` on an always-emitted key | defensive-noise | −2 |
| model-live | stale `CompletionCategory` / `ForFaction` comments | comment | 0 |
| ui | `IsTracked`/`ToggleTracked` forwarders inlined | speculative | −8 |
| ui | stale Map.xml hover comment; stranded duplicate comment in Completion.lua | comment | −1 |
| docs-config | dead `LegacyHereZonePercentPinMixin` (both lint configs), dead tests `arg` global | dead-code | −2 |
| docs-config | README right-click behaviour; refresh-data "full checks" comment | stale-docs | 0 |
| tools | `URLError` (subclass of `OSError`) in except tuple + its import; constant `active` param | defensive / speculative | −2 |
| setup follow-up | `ruff.toml`, `.agents` added to `.pkgmeta` ignore (would have shipped in the zip) | build | +2 |

Net (audit slices): −6 lines across 11 files.

## Bugs found, not fixed

| Location | Evidence | Suggested fix |
|---|---|---|
| `tools/latest_build.py:8` | User-Agent `SkillUpForever/1.0`, copied from another addon | send `LegacyHere/1.0` |
| `Core.lua:5`, `Completion.lua:211` vs `LegacyHere.toc:13` | comments say SavedVariables arrive after all files run; toc sets `LoadSavedVariablesFirst: 1` | confirm in client, then drop the directive or the deferral |
| `Completion.lua:256` | collapsed corner overlay keeps the hidden counts text's width | measure only visible text |
| `Completion.lua:198` | completion tracker header opens the map on right-click; Legacy section uses right-click for a menu | match Tracker.lua |
| `Tracker.lua` AddModule | 5 s "couldn't add a section" warning can fire even though the AddContainer hook later attaches | cancel the warning on attach |
| `tests/model_spec.lua:152` | `OverlayRect` asserted by sum only; swapped x/y passes | assert each field |

## Unproven candidates

| Location | Candidate | What would settle it |
|---|---|---|
| `Tracker.lua:16`, `Core.lua:20` | `IndexOf` / count loop vs FrameXML `tIndexOf` / `CountTable` | confirm both exist on Interface 16001 |
| `Live.lua:263` | `FLIGHT_MASTER` restates two `SNAPSHOT_CHANGES` members | owner decision; low value |
| `Model.lua:159`, `gen_legacy.py:26`, `data_spec.lua:175` | category list in three places | a data_spec assertion tying them together |
| `Map.lua:118` vs `Live.ContinentOf` | direct-parent vs tree-walk continent membership | in-game check on nested maps |
| `Map.lua:143` | `showAreas` bypasses `ns.SavedTable` | persisted key; needs an accessor, not a move |
| `gen_legacy.py:1025` vs `:978` | two copies of the compact-or-expand render rule | diff generated output after unifying |
| `gen_legacy.py:215…891` | Criteria.Type magic numbers beside `CRITERIA_KINDS` | owner decision |
| `gen_legacy.py:430` | multi-projection entrances dropped silently | print them like other unresolved rows |
| `gen_legacy.py:653`, `Map.lua:318` | oversized functions | separate reviewed split |
| `README.md`, `tools/README.md`, `CHANGELOG.md` | wall-of-text: 144/186-word bullets; build-bound counts and session history in tools/README | owner rewrite; user-facing store copy |
| `.pkgmeta` dotfile entries | may be redundant if the packager skips dotfiles | read packager source |

## Learning captured

- Rules added: none (no repeated tool-recognisable shape).
- Project skill: `rg` needs `--hidden` for `.github`; the SavedVariables timing question; FrameXML helpers count as platform for reinvented-wheel.

## Next

`sift audit diff` after deciding the `LoadSavedVariablesFirst` question in game.
